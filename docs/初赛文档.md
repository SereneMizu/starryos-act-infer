# Pro57 — ACT 模型嵌入式推理部署（初赛文档）

## 项目概述

本课题将 ACT (Action Chunking Transformer) 模仿学习模型部署到 **StarryOS** 嵌入式操作系统上，完成图像预处理 → 模型推理 → 动作后处理的完整推理流水线。项目覆盖三个目标平台，实现从 CPU 仿真到真实 NPU/TPU 硬件的全链条适配。

| 任务   | 平台                             | 内存  | 推理后端                    | 状态 |
| ------ | -------------------------------- | ----- | --------------------------- | ---- |
| 任务一 | SG2002                           | 256MB | TPU（Sophgo CV181x BF16）   | 完成 |
| 任务二 | 香橙派 RK3588（OrangePi 5 Plus） | 4GB   | NPU（Rockchip RK3588 FP16） | 完成 |
| 任务三 | QEMU RISC-V（StarryOS）          | 350MB | CPU（ONNX Runtime）         | 完成 |

## 技术路线

```
PyTorch 模型 (.pt)
    │
    ├── ONNX Export ──► output/train/model.onnx (194 MB, FP32)
    │
    ├──► [任务三 ONNX Runtime] ──► QEMU RISC-V CPU 推理
    │
    ├──► ONNX 混合精度量化 ──► model_mixed_conv+qkv.onnx (79.5 MB)
    │       │                      (conv+qkv INT8 + 其余 FP16)
    │       ├──► [任务二 RKNN] ──► act_model_rk3588.rknn (~97 MB)
    │       │                               (RK3588 NPU FP16)
    │       └──► conv+qkv INT8 QDQ + 其余 FP16
    │
    └──► [任务一 TPU-MLIR] ──► act_model_cv181x_bf16.cvimodel (~96 MB)
                                    (SG2002 TPU BF16)
```

## 构建与复现

### 前置条件

- Python 3.12+、Docker、make、sudo
- 训练产物：`output/train/model.pt` 和 `output/dataset/`（组织方提供）

### 一键复现

```bash
# 主机 Python 环境（ONNX 导出 + 量化分析）
uv venv .venv && source .venv/bin/activate
uv pip install -r requirements.txt

# 1. ONNX 导出 + 验证
make export-onnx verify-onnx

# 2. QEMU CPU 推理测试
make test-qemu

# 3. SG2002 TPU 推理（SD 卡镜像）
make sg2002-sdcard

# 4. RK3588 NPU 推理（SD 卡镜像）
make rk3588-sdcard
```

## 任务三：QEMU RISC-V CPU 推理（基础）

### 推理程序

`act-infer-ort/` — Rust 编写的 ONNX Runtime CPU 推理程序。

- **语言**: Rust
- **交叉编译目标**: `riscv64gc-unknown-linux-musl`
- **推理后端**: ONNX Runtime（Alpine apk 安装 `onnxruntime`）
- **链接方式**: 动态链接 ONNX Runtime（rootfs 中的 `.so`）
- **可执行文件大小**: 3.3 MB（RISC-V musl，strip 后）

### 模型

- **格式**: ONNX（FP32）
- **大小**: 194 MB

### 测试流程

详见「构建与复现」章节。一键运行：

```
make test-qemu
```

### 运行结果

**系统配置**:

- QEMU RAM: 350 MB（MemTotal ≈ 334 MB）
- CPU: QEMU RISC-V（单核）
- 测试帧: 2 帧（`frame_000000.jpg`、`frame_000227.jpg`）

**时间消耗**:

| 阶段               | 耗时                       |
| ------------------ | -------------------------- |
| 模型加载           | 23.97 s                    |
| 帧 0（图像预处理） | 102 ms                     |
| 帧 0（推理）       | 18.49 s                    |
| 帧 1（图像预处理） | 81 ms                      |
| 帧 1（推理）       | 18.39 s                    |
| 图像预处理合计     | 183 ms（平均 91.5 ms/帧）  |
| 推理合计           | 36.87 s（平均 18.44 s/帧） |

QEMU RISC-V 无硬件加速，CPU 运行 Transformer 模型（hidden_dim=512, FFN=3200, 8 层）速度较慢为正常现象。

**内存消耗**（实测 `--track-mem`，MemFree 差值法，后台 10ms 轮询）：

| 阶段       | MemFree    | 已用内存（较启动基线） | 较上阶段增加 |
| ---------- | ---------- | ---------------------- | ------------ |
| 启动基线   | 321,030 kB | —                      | —            |
| 模型加载后 | 45,642 kB  | 269 MB                 | +269 MB      |
| 2 帧推理后 | 37,105 kB  | 277 MB                 | +8 MB        |

内存追踪全过程（加载 + 推理）**峰值内存 277 MB**，程序退出时 MemAvailable 仍有 85 MB 余量。

| 指标                 | 值                   |
| -------------------- | -------------------- |
| QEMU 配置内存        | 350 MB               |
| MemTotal（内核可见） | 342,024 kB（334 MB） |
| **内存占用峰值**     | **277 MB**           |
| 模型加载后稳态占用   | 269 MB               |
| 单帧推理额外内存     | ~4 MB                |
| 最低可运行内存配置   | 350 MB               |

内存数据来源于 StarryOS 内核 `/proc/meminfo`，反映真实物理页使用情况。峰值通过后台线程每 10ms 轮询 `MemFree` 捕获最小值计算：`peak = (baseline_free - min_free) / 1024`（MB）。

**推理正确性**:

| 帧               | left_vel  | right_vel | 转向  | 参考转向 | 判定 |
| ---------------- | --------- | --------- | ----- | -------- | ---- |
| frame_000000.jpg | -0.001582 | +0.007263 | LEFT  | LEFT     | ✅ OK |
| frame_000227.jpg | +0.006104 | -0.000444 | RIGHT | RIGHT    | ✅ OK |

- 转向匹配率：2/2（100%）
- 最大 left_vel 偏差：0.000013
- 最大 right_vel 偏差：0.000027

## 任务一：SG2002 TPU 推理

### 推理程序

`act-infer-tpu/` — Rust 编写的 TPU 推理程序。

- **语言**: Rust
- **交叉编译目标**: `riscv64gc-unknown-linux-musl`
- **推理后端**: CVIRuntime（Sophgo TPU SDK）
- **FFI 绑定**: build.rs 中 bindgen 编译时生成，手动裁剪
- **链接方式**: 静态链接 `libcviruntime-static.a` + `libcvikernel-static.a` + `libcvimath-static.a` + `libstdc++`，动态链接 `libgcc_s.so.1`
- **可执行文件大小**: ~数 MB（静态链接）

### 模型

- **格式**: cvimodel（TPU-MLIR BF16）
- **大小**: ~96 MB
- **量化方式**: BF16（权重 BF16，输入/输出 FP32）
- **处理器**: cv181x

### 模型量化

SG2002 的 TPU（Sophgo CV181x）支持 BF16 计算。使用 TPU-MLIR 工具链将 FP32 ONNX 量化为 BF16 cvimodel，由 `make sg2002-sdcard` 自动执行：

流程：

1. `model_transform.py`：ONNX → MLIR 中间表示，指定输入 shape `[[1,1,3,224,224], [1,2]]`，用测试帧做验证
2. `model_deploy.py`：MLIR → BF16 cvimodel，处理器 cv181x，输入保留 FP32，内部计算和权重使用 BF16
3. 模型大小从 194 MB（FP32 ONNX）压缩至 **~96 MB**（2×压缩）

BF16 相比 INT8 的优势：ACT 模型的 FFN 层对 INT8 极度敏感（见下文敏感度分析），BF16 保留了近 FP32 的动态范围，在 SG2002 上达到 94.7% 的转向匹配率。

### 编译流程

```
make sg2002-sdcard
```

1. TPU-MLIR 容器中 `model_transform.py` + `model_deploy.py` 将 ONNX → BF16 cvimodel
2. 编译 StarryOS 内核（`licheerv-nano-sg2002.toml`，`plat_dyn=true`）
3. 交叉编译 `act-infer-tpu`，静态链接 CVI 库
4. 收集运行时库（`libcviruntime.so` 等）
5. 打包 SD 卡镜像（1GB，boot + ext4 rootfs）

### 运行结果

**硬件环境**:

- 板卡: SG2002
- 内存: 256 MB
- TPU: Sophgo CV181x 内置 NPU，BF16

**时间消耗**:

| 指标       | 值       |
| ---------- | -------- |
| 总帧数     | 666      |
| 总耗时     | 301.40 s |
| 平均帧耗时 | 452.6 ms |
| 最小帧耗时 | 389 ms   |
| 最大帧耗时 | 719 ms   |

**内存消耗**:

- 峰值内存（VmSize）: 114 MB

**推理正确性**:

| 指标                | 值               |
| ------------------- | ---------------- |
| 转向匹配率          | 631/666（94.7%） |
| 转向不一致          | 35/666           |
| 最大 left_vel 偏差  | 0.001925         |
| 最大 right_vel 偏差 | 0.001498         |

35 帧不一致均为左右轮速差 < 0.01 的边界帧，BF16 量化引入的微小偏差导致方向翻转，不影响实际控制决策。

### SG2002 平台的关键工程挑战

1. **256MB 内存限制**: BF16 cvimodel ~96MB，静态链接推理程序，合计约 114MB 峰值，在 256MB 可用范围内
2. **tgoskits 静态平台崩溃**: 上游 TPU/ION 设备注册只在 `plat_dyn=false` 下生效，但静态平台在 SG2002 上 NULL 函数指针崩溃。改为 `plat_dyn=true` + 放开 cfg gate 解决

## 任务二：RK3588 NPU 推理（进阶）

### 推理程序

`act-infer-rknn/` — Rust 编写的 RKNN 推理程序。

- **语言**: Rust
- **交叉编译目标**: `aarch64-unknown-linux-gnu`（glibc，匹配 `librknnrt.so` 的 NEEDED）
- **推理后端**: RKNN Runtime（Rockchip NPU SDK）
- **FFI 绑定**: build.rs 中 bindgen 编译时从 `include/rknn_api.h` 生成
- **链接方式**: 动态链接 `librknnrt.so`
- **可执行文件大小**: ~数 MB

### 模型

- **格式**: RKNN
- **大小**: ~97 MB
- **精度**: FP16（权重 FP16，输入/输出 FP32，NPU 以 FP16 执行）

### 模型量化

RK3588 NPU 硬件不支持真 FP32 计算，所有权重和激活以 FP16 执行。量化流程由 `make build-rknn` 自动执行：

流程：

1. `convert_onnx_fp16.py`：使用 `onnxconverter_common.float16` 将模型所有权重从 FP32 转为 FP16。保持输入/输出层为 FP32（`keep_io_types=True`），上游调用方无需改动。模型从 194 MB 压缩至 **~97 MB**（2×）
2. `rknn_compile.py`：加载 FP16 ONNX，`rknn.build(do_quantization=False)` 仅转换格式为 RKNN，不做 INT8 量化。NPU 以 FP16 执行，精度损失极小

### 编译流程

```
make rk3588-sdcard
```

1. 编译 StarryOS 内核（RK3588 board 配置）
2. 交叉编译 `act-infer-rknn`，动态链接 `librknnrt.so`
3. 收集运行时库
4. 打包 SD 卡镜像（2GB，boot + ext4 rootfs）

### 运行结果

**硬件环境**:

- 板卡: OrangePi 5 Plus
- SoC: Rockchip RK3588（8 核 aarch64）
- NPU: Rockchip RK3588 内置 NPU，FP16 计算

**时间消耗**:

| 指标       | 值       |
| ---------- | -------- |
| 总帧数     | 666      |
| 总耗时     | 605.66 s |
| 平均帧耗时 | 909.4 ms |
| 最小帧耗时 | 896 ms   |
| 最大帧耗时 | 996 ms   |

**内存消耗**:

- 峰值内存（VmSize）: 193 MB
- 退出时系统 MemFree: 3665 MB

**推理正确性**:

| 指标                | 值               |
| ------------------- | ---------------- |
| 转向匹配率          | 661/666（99.2%） |
| 转向不一致          | 5/666            |
| 最大 left_vel 偏差  | 0.001321         |
| 最大 right_vel 偏差 | 0.001107         |

5 帧不一致（frame_000008、000009、000010、000011、000379）均为左右轮速差极小的边界帧。

### RK3588 平台的关键工程挑战

1. **双 Python venv**: RKNN toolkit2 仅支持 Python 3.12 + onnx==1.16.1，与主机 ONNX 导出环境冲突，拆为 `.venv`（3.14，ONNX 生态）和 `.venv-rknn`（3.12，rknn-toolkit2）
2. **RKNPU 驱动 DTB 兼容性**: 必须用 tgoskits 配套 DTB（`orangepi-5-plus.dtb`），Armbian 的 DTB compatible 不匹配导致 NPU probe 失败
3. **混合精度 RKNN 编译**: 不要用 `do_quantization=True`，RKNN 的 w8a8 会强制量化输入图像，精度崩到 68%。正确做法是 ONNX 侧先量化 conv+qkv（QDQ 标注），RKNN 直转不重新量化
4. **SD 卡 IO 瓶颈**: 开发板的 SD 卡 IO 速度限制了帧加载和模型读取，实测推理中 IO 等待占比较高，后续应优化 IO 流水线

## 整体方案与代码说明

### 推理流水线

三个推理程序（ort/tpu/rknn）共享统一的流水线结构：

```
JPEG 帧 ──► 图像预处理 ──┐
                          ├──► 模型推理 ──► 动作后处理 ──► 转向判定
stats.json ──► 状态归一化 ─┘
```

**图像预处理**: 将 JPEG 解码为 RGB，resize 到 224×224，ImageNet 归一化（mean=[0.485,0.456,0.406], std=[0.229,0.224,0.225]）。ort 和 rknn 用 `image` crate 实现，tpu 用 `jpeg-decoder` 手动处理。

**状态归一化**: 从 `stats.json` 读取 QUANTILES 参数，将原始状态映射到 [-1, 1]：`2 * (x - q01) / (q99 - q01) - 1`。推理时固定输入 `state=[0.0, 0.0]`。

**动作后处理**: 模型输出 FP32 动作向量，反归一化后取第一个时间步的 `[left_vel, right_vel, gripper_target]`，根据 `left_vel < right_vel` 判定转向方向。

### CLI 接口

三个程序使用统一的命令行参数：

```
--model <path>      模型文件路径
--dir <path>        帧图片目录
--stats <path>      stats.json 路径
--reference <path>  参考结果（可选，用于验证）
--track-mem         启用内存追踪（可选）
```

### 代码组织

**act-infer-ort**（QEMU）:

| 函数 | 作用 |
|------|------|
| `load_model()` | ONNX Runtime 加载模型 |
| `preprocess_image()` | 图像解码 + resize + 归一化，返回 `ort::Tensor` |
| `infer_all()` | 逐帧推理：预处理 → session.run() → 反归一化 → 转向判定 → 与参考对比 |
| `print_summary()` | 汇总输出推理统计、内存峰值、验证结果 |
| `MemTracker` | 后台线程每 10ms 采样 MemFree，追踪最小值计算峰值 |

**act-infer-tpu**（SG2002）:

| 函数/模块 | 作用 |
|-----------|------|
| `TPUModel` | 封装 CVIRuntime：`CVI_NN_RegisterModel` 加载模型，`CVI_NN_Forward` 执行推理，`Drop` 自动释放 |
| `bf16_to_f32()` | BF16 转 FP32（左移 16 位） |
| `read_bf16_from_tensor()` | 读取 TPU BF16 输出并转为 FP32 |
| 其余模块 | 与 ort 共享相同的图像预处理、后处理、内存追踪逻辑 |

**act-infer-rknn**（RK3588）:

| 函数 | 作用 |
|------|------|
| `build.rs` | bindgen 从 `rknn_api.h` 编译时生成 FFI 绑定 |
| 推理流程 | `rknn_init()` 加载模型 → `rknn_run()` 执行 → `rknn_get_outputs()` 取结果 |
| 其余模块 | 与 ort 共享相同的预处理、后处理逻辑 |

### 内存追踪

三个程序统一使用 **MemFree 差值法**。后台线程每 10ms 读取 `/proc/meminfo` 的 MemFree，取最小值，峰值 = `(baseline_free - min_free) / 1024`（MB）。不使用 VmSize 因为 StarryOS 的 VMRSS 当前等于 VmSize，不反映真实物理内存。

### 验证系统

`--reference` 指定参考 JSON，每帧输出时自动对比转向方向并标记 OK/DIFF。最终 SUMMARY 输出转向匹配率、不一致帧数、速度偏差最大值。

## ONNX 导出与验证

### ONNX 导出

`scripts/export_onnx.py` 将 PyTorch checkpoint（`.pt`）导出为 ONNX 格式。

关键处理：ACT 模型在推理时通常从 CVAE 的近似后验采样隐变量 `z`。为消除随机性使推理可复现，导出时通过 `ACTInferenceWrapper` 将 CVAE 替换为零 latent 的确定性路径，使 ONNX 模型成为纯确定性函数：`action = f(image, state)`。

```bash
make export-onnx   # → output/train/model.onnx (194 MB)
```

- 导出方式：`torch.onnx.export` + `dynamo=True`，opset 18
- 输入：`image` [1,3,224,224] + `state` [1,2]（FP32）
- 输出：`action` [1,8,3]（FP32）

### 数值验证

`scripts/verify_onnx.py` 加载 PyTorch 和 ONNX 模型，对同一输入对比输出数值，确保导出无损：

```bash
make verify-onnx
```

## 量化策略总结

项目涉及三种量化策略，分别对应三个平台的计算能力和资源约束：

| 策略                 | 平台           | 权重精度  | 计算精度  | 模型大小 | 压缩比 | 转向匹配率 |
| -------------------- | -------------- | --------- | --------- | -------- | ------ | ---------- |
| BF16                 | SG2002 TPU     | BF16      | BF16      | 96 MB    | 2.0×   | 94.7%      |
| FP16                 | RK3588 NPU     | FP16      | FP16      | 97 MB    | 2.0×   | 99.2%      |
| conv+qkv INT8 + FP16 | x86/ARM (VNNI) | INT8/FP16 | INT8/FP16 | 79.5 MB  | 2.5×   | 99.1%      |

### BF16 量化（SG2002 TPU）

ONNX → TPU-MLIR `model_deploy.py --quantize BF16` → cvimodel。BF16 保留接近 FP32 的动态范围，适合 FFN 敏感的 ACT 模型。在 256MB 内存的 SG2002 上达到 94.7% 转向匹配率。

### FP16 量化（RK3588 NPU）

ONNX FP32 → `convert_onnx_fp16.py`（权重 FP16，输入/输出保持 FP32）→ `rknn_compile.py`（`do_quantization=False`）→ RKNN。RK3588 NPU 硬件以 FP16 执行，精度损失极小。

### 混合精度量化（conv+qkv INT8 + 其余 FP16）

为进一步压缩模型，对 ONNX 模型进行了系统性敏感度分析（详见 `docs/quantization_v2.md`）。

**方法**：对 ACT 模型的 5 类算子逐一单独做 INT8 静态量化（其余保持 FP32），在 666 帧全量评估转向匹配率下降程度：

| 类别               | 算子数 | 单类 INT8 后转向匹配率下降 | 结论            |
| ------------------ | ------ | -------------------------- | --------------- |
| QKV                | 32     | 0.90%                      | 可 INT8         |
| Conv               | 21     | 1.95%                      | 可 INT8         |
| Attention 输出投影 | 11     | 2.25%                      | 边界，保守 FP16 |
| FFN1 (512→3200)    | 8      | 5.71%                      | 必须 FP16       |
| FFN2 (3200→512)    | 8      | 15.47%                     | 必须 FP16       |

**关键发现**：FFN（尤其是 FFN2 降维层）是 ACT 模型的精度瓶颈，INT8 量化误差经残差连接放大。Conv 和 QKV 对 INT8 容忍度高。

**最终方案**：conv+qkv INT8 + 其余 FP16（模型 202 MB → 79.5 MB，2.5×，转向匹配率 99.1%）。

混合量化模型节点数从 677 增至 910（+233），原因：

- **MatMul+Add → Gemm 融合**（quant_pre_process）：MatMul 79→22，Add 109→48，Gemm 13→62（融合后的 Gemm 统一量化）
- **INT8 QDQ 包裹**：每个 INT8 算子的每个输入输出需一对 QuantizeLinear/DequantizeLinear（共 312 个 QDQ）
- **FP16/INT8 类型转换**：混合精度边界需 Cast 节点（148 个）
- **per-channel 量化**：Reshape 66→126（处理 scale/zero_point 形状）

### x86 主机混合量化测试

使用 `make quant-all`（CPU Execution Provider，666 帧全量）获得验证结果：

| 指标                | FP32 ONNX（参考） | Mixed conv+qkv INT8    |
| ------------------- | ----------------- | ---------------------- |
| 模型大小            | 202 MB            | 79.5 MB（2.5×）        |
| 推理速度            | 43 ms/帧          | **35 ms/帧**（快 19%） |
| 转向匹配率          | 100%              | **656/666（98.5%）**   |
| 转向不一致          | 0                 | 10/666                 |
| 最大 left_vel 偏差  | —                 | 0.003552               |
| 最大 right_vel 偏差 | —                 | 0.002559               |

10 帧不一致均为左右轮速差 < 0.006 的边界帧（FP32 参考下左右速度本就接近），INT8 量化引入的微小偏差导致方向翻转，不影响实际控制决策。混合量化在 x86 CPU 上比 FP32 更快（AVX2 INT8 指令加速 Conv/Gemm），模型更小、推理更快、精度几乎无损。

### QEMU 混合量化测试与 ONNX Runtime 图优化

在 QEMU RISC-V 上测试混合量化模型时发现 ONNX Runtime 的图优化问题：

**问题**：ONNX Runtime 默认启用 Level3 图优化，混合量化模型有 910 个节点（其中 460 个 Q/DQ/Cast 节点），图优化在单核 RISC-V CPU 上耗时过长（>10min 未完成），导致模型加载看似卡死。

**解决方案**：加载时禁用图优化（`GraphOptimizationLevel::Disable`），模型加载降至 17s。但推理阶段因 460 个 QDQ 节点未被优化融合，推理速度稍慢（35.8s/帧 vs FP32 的 33.5s/帧）。

| 模型                  | 节点数 | 图优化  | 加载时间     | 推理时间 |
| --------------------- | ------ | ------- | ------------ | -------- |
| FP32 ONNX             | 677    | Level3  | 23s          | 18s      |
| FP32 ONNX             | 677    | Disable | 23s          | 18s      |
| Mixed (conv+qkv INT8) | 910    | Level3  | 超时(>10min) | —        |
| Mixed (conv+qkv INT8) | 910    | Disable | 17s          | 36s      |

FP32 模型的图优化效果有限（加载/推理时间无差异），而混合模型必须禁用图优化才能加载。最终 QEMU 推理仍使用 FP32 模型。对比旧版数据，本次测试加载时间从 34s 降至 23s、推理从 34s 降至 18s，主要受益于 ONNX Runtime 版本升级带来的性能优化。

## 项目工程结构

```
starryos-act-infer/
├── act/                          # ACT 模型定义（PyTorch，组织方提供）
├── act-infer-ort/                # QEMU CPU 推理程序（Rust）
│   ├── Cargo.toml
│   └── src/main.rs
├── act-infer-tpu/                # SG2002 TPU 推理程序（Rust）
│   ├── Cargo.toml
│   ├── build.rs                  # bindgen 编译时生成 FFI 绑定 + 链接指令
│   ├── include/
│   │   ├── cviruntime.h
│   │   └── cvitpu_debug.h
│   └── src/main.rs
├── act-infer-rknn/               # RK3588 NPU 推理程序（Rust）
│   ├── Cargo.toml
│   ├── build.rs                  # bindgen 编译时生成 RKNN FFI 绑定
│   ├── include/rknn_api.h        # RKNN API 头文件
│   └── src/main.rs
├── scripts/
│   ├── batch_infer_onnx.py       # ONNX 批量推理 + verify
│   ├── batch_infer_torch.py      # PyTorch 批量推理
│   ├── build-lrzsz.sh            # lrzsz 工具编译
│   ├── build-rk3588-sdcard.sh    # RK3588 SD 卡镜像打包
│   ├── build-sg2002-sdcard.sh    # SG2002 SD 卡镜像打包
│   ├── calib_compare.py          # 校准策略对比
│   ├── convert_onnx_fp16.py      # ONNX FP16 转换
│   ├── cuda-env.sh               # CUDA 环境配置
│   ├── export_onnx.py            # ONNX 模型导出
│   ├── extract_test_reference.py # 测试参考帧提取
│   ├── install-ort.sh            # ONNX Runtime 安装
│   ├── per_layer_sensitivity.py  # 逐层敏感度分析
│   ├── prepare-app-files.sh      # QEMU 测试文件准备
│   ├── prepare-rootfs.sh         # Alpine rootfs + onnxruntime
│   ├── quantize_mixed_onnx.py    # 混合精度量化
│   ├── rknn_compile.py           # ONNX → RKNN 编译
│   ├── sensitivity_analysis.py   # 按类别敏感度分析
│   ├── tpu_compile.py            # ONNX → cvimodel
│   └── verify_results.py         # 推理结果验证
├── starry-apps/
│   ├── act-infer/                # QEMU app 配置
│   │   ├── act-infer-ort         # 推理程序二进制（预编译）
│   │   ├── act-infer-test.sh     # QEMU 内启动脚本
│   │   ├── build-riscv64gc-unknown-none-elf.toml
│   │   ├── frames/
│   │   │   ├── frame_000000.jpg
│   │   │   └── frame_000227.jpg
│   │   ├── model.onnx
│   │   ├── prebuild.sh           # rootfs overlay 注入
│   │   ├── qemu-riscv64.toml     # QEMU 运行配置
│   │   ├── reference.json
│   │   └── stats.json
│   ├── act-infer-tpu/            # SG2002 rootfs 文件
│   │   ├── infer.sh
│   │   └── lib/
│   │       ├── libcviruntime.so
│   │       ├── libcvikernel.so
│   │       ├── libcvimath.so
│   │       ├── libgcc_s.so.1
│   │       └── libstdc++.so.6
│   └── act-infer-rknn/           # RK3588 rootfs 文件
│       ├── infer.sh
│       └── lib/librknnrt.so
├── sdboot/                       # 启动镜像
│   ├── sg2002-boot.img
│   ├── rk3588-boot.img
│   └── rk3588-orangepi-5-plus.dtb
├── tgoskits/                     # StarryOS 内核（git submodule）
├── docs/
│   ├── quantization_v2.md        # 混合精度量化文档
│   ├── rk3588.md                 # RKNN 部署文档
│   ├── sg2002.md                 # TPU 部署文档
│   └── superpowers/              # AI 辅助配置
├── output/
│   ├── dataset/                  # 训练数据集
│   ├── train/
│   │   ├── model.pt              # PyTorch 检查点
│   │   ├── model.onnx            # FP32 ONNX 模型
│   │   ├── model_fp16.onnx       # FP16 ONNX 模型
│   │   └── model_mixed_*.onnx    # 混合精度变体
│   ├── test/reference.json       # 测试参考帧（2 帧）
│   ├── tpu/                      # cvimodel 产物
│   ├── rknn/                     # RKNN 模型产物
│   ├── sg2002/                   # SD 卡镜像 + bootchain
│   ├── rk3588/                   # SD 卡镜像 + bootchain
│   ├── infer_results_onnx.json   # ONNX 推理结果
│   └── infer_results_torch.json  # PyTorch 推理结果
├── Makefile                       # 统一构建入口
├── AGENTS.md                     # AI 辅助开发指南
├── README.md                     # 项目说明
├── requirements.txt              # Python 依赖（主机）
├── requirements-rknn.txt         # Python 依赖（RKNN）
├── download_data.ipynb           # 数据下载
├── infer.ipynb                   # 推理 notebook
├── train.ipynb                   # 训练 notebook
├── check*_*.onnx                 # ONNX 图优化检查点
└── tmp/                          # 运行时临时数据
```

## AI 使用说明

我对 ACT 模型尚不熟悉，因此模型相关的部分工作借助 AI 完成。构建系统、rootfs 准备、StarryOS 集成、TPU/NPU 适配等工程性工作由我完成。使用的工具为 OpenCode，主要模型为 GLM-5.1 / DeepSeek-V4。

### AI 辅助完成的部分

- **部分代码编写**: Rust 推理程序 `act-infer-ort/src/main.rs`（ONNX Runtime 调用、图像预处理、动作反归一化、转向判定）、ONNX 导出 `scripts/export_onnx.py`（`ACTInferenceWrapper`、零 latent 确定性推理）、ONNX 验证 `scripts/verify_onnx.py`（PyTorch vs ONNX 数值对比）的代码实现
- **混合精度量化脚本**: `sensitivity_analysis.py`、`per_layer_sensitivity.py`、`calib_compare.py`、`quantize_mixed_onnx.py` 的代码实现
- **FFI bindings 生成**: bindgen 配置、绑定裁剪策略
- **部分 Makefile 规则**: 由 AI 协作完成
- **部分文档整理**: 本文档由 AI 根据项目代码和文档整理

### 人工完成的部分

- **项目方案设计**: 整体架构设计（QEMU + SG2002 + RK3588 三平台）、技术选型
- **推理流水线与模型导出方案设计**: 主导 Rust 推理程序整体流程设计、ONNX 确定性导出策略及验证方案
- **混合精度量化方案设计**: 敏感度分析方法设计（按类别 + 逐层）、校准策略对比设计、结果分析
- **Rust 推理程序架构设计**: 三套推理程序（ort/tpu/rknn）整体架构、统一 CLI 接口、内存追踪模块设计
- **tgoskits 内核适配**: SG2002 TPU/ION cfg gate 修复（`plat_dyn=true` 下设备注册）、RK3588 NPU board 配置、DTB 兼容性处理
- **TPU 模型编译**: `tpu_compile.py` 调用 TPU-MLIR 工具链（model_transform + model_deploy）
- **交叉编译环境搭建**: Docker 容器方案、多架构工具链配置、glibc vs musl 选择
- **SD 卡镜像制作**: `build-sg2002-sdcard.sh`、`build-rk3588-sdcard.sh` 脚本编写
- **rootfs 准备**: `prepare-rootfs.sh`（chroot + apk 注入 ONNX Runtime）
- **Makefile 构建系统**: 统一构建入口、docker 编排、国内镜像源配置
- **硬件调试**: SG2002 板级启动调试（u-boot → StarryOS）、RK3588 U-Boot 引导链、NPU probe 故障排查
- **AI 生成代码的审查与修改**: 所有 AI 生成代码均经过人工审查、测试验证和修改

### 非本队来源说明

本项目的构建基于以下外部依赖，在此明确说明其来源和用途：

| 来源 | 用途 | 类型 |
|------|------|------|
| [StarryOS / tgoskits](https://github.com/rcore-os/tgoskits) | 操作系统内核、驱动框架、构建系统（xtask）、QEMU 运行环境 | git submodule，上游开源 |
| [ONNX Runtime](https://onnxruntime.ai) | QEMU 推理后端，通过 Alpine apk 安装 | 第三方库，动态链接 |
| [ort crate](https://crates.io/crates/ort) | Rust 的 ONNX Runtime FFI 绑定 | Rust crate |
| [Sophgo TPU-MLIR](https://github.com/sophgo/tpu-mlir) | ONNX → cvimodel 编译工具链 | 第三方工具，Docker 容器 |
| [Sophgo CVIRuntime](https://www.sophgo.com) | SG2002 TPU 推理 SDK（静态链接 `libcviruntime-static.a` 等） | 芯片厂商 SDK |
| [Rockchip RKNN Toolkit2](https://github.com/airockchip/rknn-toolkit2) | ONNX → RKNN 编译工具链 | 第三方工具，Python venv |
| [Rockchip RKNN Runtime](https://github.com/airockchip/rknn-toolkit2) | RK3588 NPU 推理 SDK（动态链接 `librknnrt.so`） | 芯片厂商 SDK |
| [clap crate](https://crates.io/crates/clap) | Rust CLI 参数解析 | Rust crate |
| [image crate](https://crates.io/crates/image) | 图像解码和 resize | Rust crate |
| [ndarray crate](https://crates.io/crates/ndarray) | ONNX 张量操作 | Rust crate |
| [bindgen](https://crates.io/crates/bindgen) | C 头文件 → Rust FFI 绑定自动生成 | Rust 构建工具 |
| [PyTorch](https://pytorch.org) | 模型导出、验证、敏感度分析 | Python 库 |
| [ONNX](https://onnx.ai) / `onnx` / `onnxscript` | ONNX 模型操作和量化 | Python 库 |
| `tgoskits/apps/starry/` 中已有 app 示例 | `prebuild.sh`、`qemu-riscv64.toml`、`build-*.toml` 等配置文件的写法参考 | 同一仓库内的参考实现 |
| 赛题组织方 | `act/` 模型定义、`.pt` 检查点、数据集、`infer.ipynb` 参考实现 | 比赛提供资源 |

## 下一步任务

### 板侧混合精度推理

当前混合精度量化（conv+qkv INT8 + 其余 FP16）仅在 x86 主机上完成验证，尚未部署到目标板。后续工作：

- **SG2002 板侧推理**: 将模型部分量化（conv+qkv INT8 + 其余 BF16）通过 TPU-MLIR 工具链编译为 cvimodel，利用 TPU 的 BF16 计算单元承载敏感层，INT8 单元承载 conv+qkv，降低模型体积并提升推理精度
- **RK3588 板侧推理**: 将 ONNX 侧量化后的混合精度模型输入 RKNN 工具链编译，NPU 以 INT8/FP16 混合精度执行，进一步压缩模型大小并提升推理速度

### 性能优化

- **推理速度**: 通过模型算子融合、内存复用、流水线并行等方式缩短推理时间
- **内存占用**: 通过逐层加载、共享缓冲区、及时释放中间张量等方式降低峰值内存
- **SD 卡 IO 优化**: 开发板 SD 卡读写速度有限，帧加载和模型读取存在 IO 等待，通过异步预加载、双缓冲、内存缓存等方式优化 IO 流水线
- **QEMU 混合量化**: 探索 ONNX Runtime 自定义图优化策略，在避免超时的前提下融合混合模型的 QDQ 节点以提升推理速度

### 精度提升

- **SG2002 BF16 校准优化**: 分析 35 帧边界误判原因，改进校准数据集覆盖策略
- **混合量化校准**: 在 TPU/NPU 上复现主机混合精度流程，端到端验证精度损失

### 工程完善

- **自动化测试**: 建立 CI 流水线，板侧推理结果自动对比参考并生成报告
- **统一推理框架**: 将三套推理程序（ort/tpu/rknn）的公共部分（图像预处理、动作后处理、内存追踪）抽取为共享库，减少代码重复


